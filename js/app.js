/* =====================================================================
   app.js — 啟動進入點
===================================================================== */

(async function init() {
  Router.init();
  await Router.go('home');
})();
